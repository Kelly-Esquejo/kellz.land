import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    // Comment the followig 1 line of code to run website locally
    // output: "export",
    basePath: "/kellz.land",
    // assetPrefix: "/kellz.land/",
};

export default nextConfig;
